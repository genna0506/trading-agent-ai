# Core data stack
pandas>=2.0,<3.0
numpy>=1.26,<3.0
pyarrow>=15.0,<21.0      # parquet engine per data/processed

# Market data & FX
yfinance>=0.2.40,<0.3.0

# Financial math (XIRR / MWR via scipy.optimize)
scipy>=1.11,<2.0

# Visualizzazioni interattive
plotly>=5.20,<6.0

# Notebook / esplorazione
jupyter>=1.0,<2.0
ipykernel>=6.29,<7.0

# Testing
pytest>=8.0,<9.0

# --- Fasi successive (decommentare quando servono) ---
# Fase 4 - Agente AI + dashboard
# anthropic>=0.40,<1.0
# streamlit>=1.40,<2.0
# Fase 3 - ML
# scikit-learn>=1.4,<2.0
